median : (xs : {v : List F64 | len v > 0 and all_finite v}) -> {r : F64 | min xs <= r and r <= max xs}
median %0 =
  let %1 = sort %0 in
  let %2 = len %1 in
  let %3 = %2 / 2 in
  if %2 % 2 == 1
  then nth %1 %3
  else
    let %4 = nth %1 (%3 - 1) in
    let %5 = nth %1 %3 in
    let %6 = %4 + %5 in
    if %6 - %6 == 0.0
    then %6 / 2.0
    else %4 / 2.0 + %5 / 2.0
