median : (xs : {v : List F64 | len v > 0}) -> {r : F64 | min xs <= r and r <= max xs}
median %0 =
  let %1 = sort %0 in
  let %2 = len %1 in
  let %3 = %2 / 2 in
  if %2 % 2 == 1
  then nth %1 %3
  else (nth %1 (%3 - 1) + nth %1 %3) / 2.0
