sorted : (xs : List F64) -> Bool
sorted %0 =
  let %1 = list_len %0 in
  let rec %2 = fun %3 ->
    if %3 + 1 >= %1
    then True
    else
      if list_nth %0 %3 <= list_nth %0 (%3 + 1)
      then %2 (%3 + 1)
      else False
  in
  %2 0
