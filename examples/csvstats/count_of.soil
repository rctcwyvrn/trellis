count_of : (xs : List F64) -> (x : F64) -> I64
count_of xs x =
  let n = list_len xs in
  let rec go = fun i acc ->
    if i == n
    then acc
    else go (i + 1) (if list_nth xs i == x then acc + 1 else acc)
  in
  go 0 0
