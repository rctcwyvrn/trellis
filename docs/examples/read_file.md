# read_file

The prelude's `read_file`, slated to be the first real definition: a `.tr`
spec (prose, capability-style signature, fake-capability test, real-mode
cram test), its `.soil` lowering, and its `.lock` entry. The canonical
files live in [`examples/`](https://github.com/rctcwyvrn/trellis/tree/main/examples);
these are included verbatim.

## read_file.tr

````markdown
{{#include ../../examples/read_file.tr}}
````

## read_file.soil

```
{{#include ../../examples/read_file.soil}}
```

## read_file.lock

```json
{{#include ../../examples/read_file.lock}}
```
